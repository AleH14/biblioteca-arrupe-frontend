# Configuración de Next.js - Sistema Biblioteca Arrupe

# URL base del backend (obligatorio)
# Asegúrate de usar localhost:4000 para desarrollo local
NEXT_PUBLIC_API_URL=http://localhost:4000

# Entorno de ejecución
NODE_ENV=development

# NOTA: 
# - Las cookies refreshToken se manejan automáticamente (httpOnly)
# - El accessToken se almacena en localStorage
# - No se requieren secretos adicionales en frontend